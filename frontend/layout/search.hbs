module.exports = () => `
<div class="search-wrapper">
  <style>
    .search-form {
      position: relative;
      display: flex;
      max-width: 600px;
      margin: 40px auto 0 auto;
    }

    .search-form input[type="search"] {
      flex: auto;
      width: 100%;
      height: 40px;
      font-size: 20px;
      padding: 5px 15px;
      border: 1px solid var(--dark);
      border-right: none;
      border-radius: 5px;
    }

    .search-form button[type="submit"] {
      position: absolute;
      right: 0;
      width: 55px;
      height: 100%;
      padding: 6px;
      background: var(--dark);
      border: 1px solid var(--dark);
      border-left: none;
      border-radius: 0 5px 5px 0;
      cursor: pointer;
      transition: 0.1s ease;
    }

    .search-form button[type="submit"]:hover {
      background: var(--red);
      border-color: var(--red);
    }

    .magnifier-svg {
      width: 100%;
      height: 100%;
      stroke-width: 6px;
      stroke: var(--white);
      fill: var(--white);
    }

    /* =============== Mobile, IPads & Tablet styles =============== */
    @media screen and (max-width: 850px) {
      .search-form {
        max-width: 75%;
      }

      .search-form input[type="search"] {
        height: 35px;
        border-radius: 25px;
      }

      .search-form button[type="submit"] {
        min-width: 45px;
        width: 45px;
        height: 100%;
        padding: 7px;
        border-radius: 0 25px 25px 0;
      }
    }
  </style>
  <form action="/search" method="GET" class="search-form">
    <input type="search" name="text" class="nf">
    <button type="submit" class="magnifier-svg-wrapper" tabindex="0">
      <svg class="magnifier-svg">
        <use href="#magnifier-svg-symbol" />
      </svg>
    </button>
  </form>

  <script>

  </script>
</div>
`;