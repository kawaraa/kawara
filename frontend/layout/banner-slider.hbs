module.exports = (images = [], cssClass)=> `
<section class="slider outer ${cssClass}">
  <style>
    .slider.outer {
      position: relative;
      overflow: hidden;
      max-width: 700px;
      height: 30vh;
      margin: 30px auto 0 auto;
      border-radius: 10px;
    }

    .slider.wrapper {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      display: flex;
      transition: left 0.8s ease;
    }

    .slider.item {
      overflow: hidden;
      background-size: cover;
      background-position: center;
      width: 700px;
      height: 100%;
    }

    .slider.img {
      width: 100%;
      height: 100%;
    }

    /* =============== Mobile, IPads & Tablet styles =============== */
    @media screen and (max-width: 850px) {
      .slider.outer {
        max-width: 70%;
        height: 20vh;
      }
    }
  </style>

  <div class="slider wrapper" data-role="slider">
    <a href="/" class="slider item">
      <img src="/image/slipper.jpg" alt="" class="slider img" />
    </a>
    <a href="/" class="slider item">
      <img src="/image/slipper.jpg" alt="" class="slider img" />
    </a>
    <a href="/" class="slider item">
      <img src="/image/slipper.jpg" alt="" class="slider img" />
    </a>
    ${images.render((image) => `
    <a href="/" class="slider item">
      <img src="${image}" alt="" class="slider img" />
    </a>
    `)}
  </div>

  <script>
    const slider = query(".slider.wrapper");
    let left = 0;

    const nextImage = () => {
      if (slider.offsetLeft === 0) left = slider.children[1].offsetWidth * -1;
      if (slider.offsetLeft === left * (slider.children.length - 1)) left *= -1;
      slider.style.left = slider.offsetLeft + left + "px";
      setTimeout(nextImage, 4000);
    }
   // setTimeout(nextImage, 4000);


  </script>
</section>
`;